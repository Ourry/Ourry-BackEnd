spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: URL
    username: USERNAME
    password: PASSWORD
  jpa:
    database: mysql
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
  profiles:
    active: local